export interface ISettings{
    out: string;
    width: number;
    height: number;
    bgColor: string;
    lineColor: string;
    lineWidth: number;
    lineDistance: number;
    lineRotation: number;
}